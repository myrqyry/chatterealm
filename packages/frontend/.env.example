# Frontend Environment Variables
# Copy this file to .env.development or .env.production and fill in the values

# API Backend URL (for production deployments)
# VITE_API_URL=https://api.chatterealm.com

# WebSocket URL (for production deployments)
# VITE_WS_URL=wss://api.chatterealm.com

# Development mode uses proxy configuration in vite.config.ts
# No environment variables needed for local development
