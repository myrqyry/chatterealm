# Backend Environment Variables
# Copy this file to .env.development or .env.production and fill in the values

# Server Configuration
PORT=8081
NODE_ENV=development

# CORS Configuration
# In development, multiple origins are allowed (localhost:3000, localhost:5173)
# In production, set this to your frontend domain
# ALLOWED_ORIGINS=https://chatterealm.com

# Rate Limiting
# Window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Max requests per window (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# Game Configuration
# These are set in shared/src/constants/gameConstants.ts
# Override here if needed for specific environments
# GAME_GRID_WIDTH=100
# GAME_GRID_HEIGHT=100
# GAME_MAX_PLAYERS=100

# Twitch Configuration
# Set your Twitch API credentials and bot identity
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
TWITCH_CHANNEL_NAME=
# Bot identity for chat responses (tmi.js)
TWITCH_BOT_USERNAME=
TWITCH_OAUTH_TOKEN=
